config.yaml
fb2converters
*.lst
/target
**/target/
/sopds
/sopds-tts
/zipdupes
# NB: sopds-tts-rs/ and zipdupes-rs/ are tracked subprojects (their Rust binaries live in
# */target/, already ignored) — do NOT re-add /sopds-tts-rs or /zipdupes-rs, it hides new
# tracked files (e.g. flake.lock updates) behind an "ignored" git-add error.
sopds_backup.dump*
tags
.task/
.claude/
.claude/*
.claude/settings.local.json
*.swp
fetch/
test_baseline.txt

# python bytecode
__pycache__/
*.pyc
